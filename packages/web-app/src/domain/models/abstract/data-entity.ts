/* eslint-disable class-methods-use-this */
/* eslint-disable max-classes-per-file */

import Identifiable from './identifiable';

interface DataEntity extends Identifiable {
    name: string;
}

export default DataEntity;
