{
    "name": "@danielblignaut/aws-env-config",
    "bin": {
        "fetch-env-config": "./dist/index.js"
    },
    "version": "2.0.5",
    "scripts": {
        "start": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts",
        "watch": "tsc -w",
        "build": "tsc -p .",
        "local": "sudo npm i -g && aws-env-config"
    },
    "dependencies": {
        "@types/node": "^14.14.21",
        "aws-sdk": "2.770.0",
        "nodemon": "^2.0.7",
        "ts-node": "^9.1.1",
        "typescript": "^4.1.3",
        "yargs": "^16.2.0"
    }
}
